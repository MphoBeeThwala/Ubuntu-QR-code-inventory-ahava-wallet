# Ahava eWallet — Root Railway Configuration
# This file defines all services for the Railway monorepo.
# Each service points to its respective directory and uses local npm commands.

[services.api-gateway]
name = "api-gateway"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=api-gateway -t api-gateway ."
startCommand = "node dist/main.js"

[services.auth-service]
name = "auth-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=auth-service -t auth-service ."
startCommand = "node dist/main.js"

[services.wallet-service]
name = "wallet-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=wallet-service -t wallet-service ."
startCommand = "node dist/main.js"

[services.payment-service]
name = "payment-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=payment-service -t payment-service ."
startCommand = "node dist/main.js"

[services.notification-service]
name = "notification-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=notification-service -t notification-service ."
startCommand = "node dist/main.js"

[services.aml-service]
name = "aml-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=aml-service -t aml-service ."
startCommand = "node dist/main.js"

[services.kyc-service]
name = "kyc-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=kyc-service -t kyc-service ."
startCommand = "node dist/main.js"

[services.reporting-service]
name = "reporting-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=reporting-service -t reporting-service ."
startCommand = "node dist/main.js"

[services.ussd-service]
name = "ussd-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=ussd-service -t ussd-service ."
startCommand = "node dist/main.js"

[services.agent-service]
name = "agent-service"
sourceDirectory = "."
dockerfilePath = "Dockerfile.service"
buildCommand = "docker build -f Dockerfile.service --build-arg SERVICE_NAME=agent-service -t agent-service ."
startCommand = "node dist/main.js"

[services.pwa]
name = "pwa"
sourceDirectory = "apps/pwa"
buildCommand = "npm run build"
startCommand = "npm run start"

[services.agent-portal]
name = "agent-portal"
sourceDirectory = "apps/agent-portal"
buildCommand = "npm run build"
startCommand = "npm run start"
