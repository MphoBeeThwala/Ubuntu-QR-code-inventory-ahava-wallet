apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ahava-ingress
  annotations:
    alb.ingress.kubernetes.io/certificate-arn: "REPLACE_ACM_CERTIFICATE_ARN"
    alb.ingress.kubernetes.io/wafv2-acl-arn: "REPLACE_WAF_ACL_ARN"
