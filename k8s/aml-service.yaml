apiVersion: apps/v1
kind: Deployment
metadata:
  name: aml-service
  namespace: ahava
  labels:
    app: aml-service
    app.kubernetes.io/part-of: ahava-ewallet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aml-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: aml-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: aml-service
          image: ahava/aml-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 6007
              name: http
          envFrom:
            - configMapRef:
                name: ahava-config
            - secretRef:
                name: ahava-secrets
          env:
            - name: PORT
              value: "6007"
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 6007
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 6007
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
---
apiVersion: v1
kind: Service
metadata:
  name: aml-service
  namespace: ahava
  labels:
    app: aml-service
spec:
  type: ClusterIP
  selector:
    app: aml-service
  ports:
    - name: http
      port: 6007
      targetPort: 6007
